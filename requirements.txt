# Core Backend
fastapi
uvicorn
python-dotenv

# Database and ORM
sqlalchemy>=2.0.0
asyncpg
aiosqlite

# Security and Authentication
python-jose[cryptography]
pyjwt
pydantic[email]

# FIX: Specific versions to resolve the "AttributeError: module 'bcrypt' has no attribute '__about__'"
# passlib 1.7.4 requires a specific bcrypt range to avoid the crash you experienced.
passlib[bcrypt]==1.7.4
bcrypt==4.0.1